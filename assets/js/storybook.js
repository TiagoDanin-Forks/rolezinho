// Entry point for the Storybook iframe (see lib/rolezinho_web/storybook.ex).
// Kept separate from app.js: stories render in a sandbox that does not boot the
// application's LiveSocket.
import "phoenix_html";
