{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read f; case \"$f\" in *.ex|*.exs|*.heex) if [ -f mix.exs ]; then mix format \"$f\" || echo \"[hook] aviso: mix format falhou em $f\"; fi ;; esac; exit 0; }"
          }
        ]
      }
    ]
  }
}
